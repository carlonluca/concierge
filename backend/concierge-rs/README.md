# concierge-rs

Minimal Rust project scaffold created inside `backend`.

## Quick start

Build:

```bash
cargo build
```

Run:

```bash
cargo run
```

Notes:
- Project path: `concierge-rs`
- Rust toolchain (cargo, rustc) required to build/run
